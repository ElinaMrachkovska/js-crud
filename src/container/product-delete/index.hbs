<section>
    <div class="form">
    <form method="GET" action="/deleteById">
      <h1 class="form__title"> Список товарів</h1>
      <div class="product-list">
        {{#with data.products}}
        {{#if isEmpty}}
        <mark> Список пустий</mark>
        {{else}}
        <ul>
          {{#each list}}
          <li>{{name}} | {{price}} | {{id}} <a href="/product-delete?id={{id}}">(Видалити)</a></li>
          {{/each}}
        </ul>
      {{/if}}
        {{/with}}
        </div>
        </div>
        </section>